"""Implement revenue_by_region using SQLAlchemy Core. See the task prompt."""


def revenue_by_region(engine):
    # Reflect the `sales` table and return (region, total) tuples,
    # ordered by total descending then region ascending.
    return []
