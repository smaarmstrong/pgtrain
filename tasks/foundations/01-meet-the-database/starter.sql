-- Explore the task database first (\dt to list its tables, \d <name> to
-- describe one), then make this return EVERY row and EVERY column of the
-- table that stores the books.
-- (This starter queries the wrong table — fix it.)
SELECT * FROM members;
