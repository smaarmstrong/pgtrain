-- This query is SUPPOSED to return two columns for every plant: its name,
-- and the fixed text label houseplant. It doesn't run. Run it, read the
-- error the way the lesson showed you, and fix it.
SELECT name, "houseplant" FROM plants
